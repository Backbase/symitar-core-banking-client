# This configuration will prevent the JaCoCo plugin from accounting for Lombok-generated code when doing code coverage scans
# which could negatively impact your builds
lombok.addLombokGeneratedAnnotation = true